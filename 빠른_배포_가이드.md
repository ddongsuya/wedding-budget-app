# ⚡ 빠른 배포 가이드 (30분 완성!)

## 🎯 목표
무료로 웹사이트를 배포해서 다른 사람들이 사용할 수 있게 만들기

---

## 📋 준비물
- GitHub 계정
- 이메일 주소

---

## 🚀 단계별 가이드

### 1단계: GitHub에 코드 올리기 (5분)

#### 1-1. GitHub 계정 만들기
1. https://github.com 접속
2. "Sign up" 클릭
3. 이메일, 비밀번호 입력
4. 계정 생성

#### 1-2. 새 저장소 만들기
1. GitHub 로그인
2. 오른쪽 위 `+` → "New repository"
3. Repository name: `wedding-budget-app`
4. **Public** 선택
5. "Create repository" 클릭

#### 1-3. 코드 업로드
VS Code 터미널에서:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/당신의아이디/wedding-budget-app.git
git branch -M main
git push -u origin main
```

---

### 2단계: 데이터베이스 만들기 (10분)

#### 2-1. Supabase 가입
1. https://supabase.com 접속
2. "Start your project" 클릭
3. GitHub으로 로그인

#### 2-2. 프로젝트 생성
1. "New Project" 클릭
2. 정보 입력:
   - Name: `wedding-budget`
   - Password: 강력한 비밀번호 (꼭 저장!)
   - Region: **Northeast Asia (Seoul)**
3. "Create new project" 클릭
4. 2분 대기

#### 2-3. 데이터베이스 정보 복사
1. 왼쪽 메뉴 → "Project Settings" (톱니바퀴)
2. "Database" 클릭
3. 다음 정보 복사해서 메모장에 저장:
   - Host
   - Database name
   - Port
   - User
   - Password

#### 2-4. 테이블 생성
1. 왼쪽 메뉴 → "SQL Editor"
2. "New query" 클릭
3. VS Code에서 `backend/src/config/migrate.ts` 파일 열기
4. SQL 코드 전체 복사 (CREATE TABLE 부분)
5. Supabase에 붙여넣기
6. "Run" 클릭
7. "Success" 메시지 확인

---

### 3단계: 백엔드 배포 (7분)

#### 3-1. Railway 가입
1. https://railway.app 접속
2. "Start a New Project" 클릭
3. GitHub으로 로그인
4. Railway 권한 승인

#### 3-2. 프로젝트 배포
1. "Deploy from GitHub repo" 선택
2. `wedding-budget-app` 선택
3. "Add variables" 클릭

#### 3-3. 환경 변수 설정
다음 변수들을 하나씩 추가:

```
NODE_ENV=production
PORT=3000

# Supabase 정보 (2-3에서 복사한 것)
DB_HOST=db.xxxxxxxxxxxxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=여기에_Supabase_비밀번호

# JWT (아래 사이트에서 생성)
# https://randomkeygen.com/ → "Fort Knox Passwords" 복사
JWT_SECRET=여기에_랜덤_문자열_붙여넣기
JWT_REFRESH_SECRET=여기에_또_다른_랜덤_문자열
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
CORS_ORIGIN=*
```

#### 3-4. 배포 시작
1. "Deploy" 클릭
2. 3-5분 대기
3. "Settings" → "Domains" → URL 복사
   - 예: `https://wedding-budget-production.up.railway.app`
   - **이 URL을 메모장에 저장!**

---

### 4단계: 프론트엔드 배포 (5분)

#### 4-1. Vercel 가입
1. https://vercel.com 접속
2. "Start Deploying" 클릭
3. GitHub으로 로그인

#### 4-2. 프로젝트 가져오기
1. "Add New..." → "Project"
2. `wedding-budget-app` 선택
3. "Import" 클릭

#### 4-3. 설정
1. Framework Preset: **Vite** 선택
2. Root Directory: `./` (그대로)
3. Build Command: `npm run build`
4. Output Directory: `dist`

#### 4-4. 환경 변수
"Environment Variables" 섹션에서:
- Name: `VITE_API_URL`
- Value: `https://your-railway-url.up.railway.app/api`
  (Railway URL 뒤에 `/api` 추가!)

#### 4-5. 배포
1. "Deploy" 클릭
2. 2-3분 대기
3. "Visit" 클릭
4. 웹사이트 열림! 🎉

---

### 5단계: CORS 설정 (2분)

#### 5-1. Vercel URL 복사
- 예: `https://wedding-budget-app.vercel.app`

#### 5-2. Railway 환경 변수 수정
1. Railway 프로젝트 열기
2. "Variables" 탭
3. `CORS_ORIGIN` 찾기
4. 값을 Vercel URL로 변경
5. 저장 → 자동 재배포 (1분)

---

## ✅ 완료!

### 테스트하기
1. Vercel URL 접속
2. 회원가입
3. 로그인
4. 기능 테스트

### URL 공유하기
```
https://wedding-budget-app.vercel.app
```
이 주소를 친구들에게 공유하세요!

---

## 🎁 보너스: 커스텀 도메인

### 무료 도메인 받기
1. https://www.freenom.com 접속
2. 원하는 도메인 검색 (예: `mywedding.tk`)
3. 무료 도메인 선택
4. 회원가입 후 등록

### Vercel에 연결
1. Vercel 프로젝트 → "Settings" → "Domains"
2. 도메인 입력
3. DNS 설정 안내 따라하기

---

## 📊 모니터링

### 방문자 확인
- Vercel → "Analytics"

### 에러 확인
- Railway → "Deployments" → "View Logs"

### 데이터 확인
- Supabase → "Table Editor"

---

## 🔄 업데이트 방법

코드 수정 후:
```bash
git add .
git commit -m "업데이트"
git push
```
→ 자동으로 재배포됩니다!

---

## 💰 비용

- **모두 무료!**
- Vercel: 무료 (월 100GB)
- Railway: $5 크레딧/월
- Supabase: 무료 (500MB)

---

## 🆘 도움말

### "502 Bad Gateway"
→ Railway 환경 변수 확인

### "CORS Error"
→ CORS_ORIGIN이 Vercel URL과 일치하는지 확인

### "Database Error"
→ Supabase 비밀번호 확인

---

## 📞 문제 발생 시

1. 에러 메시지 스크린샷
2. Railway 로그 확인
3. Vercel 로그 확인
4. 저에게 물어보기!

---

**축하합니다! 🎉**
이제 전 세계 누구나 당신의 앱을 사용할 수 있습니다!
